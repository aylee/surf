.DS_Store
node_modules/
.pnpm-store/
dist/
dist-*/
.wrangler/
.vite/
coverage/
*.tsbuildinfo

.env
.env.*
!.env.example
.dev.vars*
!/apps/web/.dev.vars.example
/apps/web/wrangler.instance.jsonc
/apps/web/.worker-secrets

.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

data/raw/
data/tmp/

# Local Analysis model bakeoff inputs/results can contain forecast payloads and
# model output. They are reproducible operator artifacts, not repository data.
.analysis-bakeoff/
apps/narrative-runner/bakeoff.local.json

# Local planning and agent session state are not part of the public product.
# Match both the legacy directory and the machine-local symlink entry.
/cc_state
# Browser-automation scratch output from local UI verification.
/.playwright-mcp/
pr-b-*.png
