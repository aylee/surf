# Local Worker-only secrets. Copy this file to .dev.vars and keep the copy private.
# Gemini is shared by the dormant v2 rollback Agent and the bounded Analysis
# cloud watchdog. Keep the real key only in .dev.vars / Wrangler secrets.
GEMINI_API_KEY=replace_with_your_google_ai_studio_key
FORECAST_BRIEF_ENABLED=false
# Active Analysis is separately disabled until both Queues and the local runner exist.
NARRATIVE_ENABLED=false
NARRATIVE_RESULT_TOKEN=replace_with_a_dedicated_result_token
