{
  "name": "@surf/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit && pnpm seed:check",
    "seed:generate": "tsx scripts/generate-norcal-seed.ts",
    "seed:check": "tsx scripts/generate-norcal-seed.ts --check",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@surf/forecast-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "tsx": "4.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
